import SwiftUI

struct ProfileView: View {
    var body: some View {
        NavigationView {
            Text("Me")
                .navigationTitle("Me")
        }
    }
}

#Preview {
    ProfileView()
} 