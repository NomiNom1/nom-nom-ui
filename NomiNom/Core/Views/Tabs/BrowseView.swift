import SwiftUI

struct BrowseView: View {
    var body: some View {
        NavigationView {
            Text("Browse")
                .navigationTitle("Browse")
        }
    }
}

#Preview {
    BrowseView()
} 